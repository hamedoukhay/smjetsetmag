{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Membre list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Username</th>
                <th>Password</th>
                <th>Email</th>
                <th>Isadministrateur</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('membre_show', { 'id': entity.id }) }}">{{ entity.nom }}</a></td>
                <td>{{ entity.username }}</td>
                <td>{{ entity.password }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.isadministrateur }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('membre_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('membre_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('membre_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
